{
  "name": "@swoop/connector",
  "version": "0.1.0",
  "private": true,
  "description": "Puma data + side-effect connector. Exposes data tools (search/get_detail/illustrate — chunk C) and side-effecting tools (handoff_submit — chunk E) over MCP-over-HTTP. Today: handoff side-effects only; data tools land with chunk C.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@swoop/common": "*",
    "nodemailer": "^8.0.7",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/nodemailer": "^8.0.0",
    "vitest": "^2.1.0"
  }
}
