// product/ui/vitest.setup.ts
//
// Global vitest setup: installs `@testing-library/jest-dom` matchers so
// assertions like `toBeInTheDocument()` work inside test files.

import "@testing-library/jest-dom/vitest";
